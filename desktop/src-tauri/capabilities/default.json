{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "SoundLink 主窗口默认能力：核心命令 + opener 插件。",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
