import UIKit

// 项目已移除 Scene 模式（Info.plist 中无 UIApplicationSceneManifest），
// 改用传统 AppDelegate 模式。此文件保留以避免 Xcode 报缺失文件，
// 但不再使用。插件注册在 AppDelegate 中完成。
class SceneDelegate: UIResponder, UIWindowSceneDelegate {
    var window: UIWindow?
}
